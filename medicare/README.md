# medicare

A new Flutter project.

## Analysis Options

This project uses the `dart_code_metrics` package to analyze the code.

To run the analysis, run the following command:
```
dart pub global run dart_code_metrics:metrics lib --reporter=json > metricas_cenario_A.json
```